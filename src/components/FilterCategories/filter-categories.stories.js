import React from 'react';
import FilterCategory from './filter-categories';

export default {
  component: FilterCategory,
  title: 'Components/FilterCategory',
};

export const Default = () => <FilterCategory />;